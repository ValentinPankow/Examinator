// VP

$(document).ready(function() {
    let helpText = "\
    <h5>Klassenübersicht</h5>\
    <p>Auf dieser Seite werden alle geplanten Klausuren der Klasse angezeigt.</p>\
    ";
    $('#helpText').html(helpText);
});